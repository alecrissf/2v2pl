#pragma once

#include <cstddef>

namespace sgbd
{

typedef std::size_t    usize;
typedef unsigned int   uint;
typedef unsigned short ushort;
typedef unsigned char  ubyte;

constexpr const usize npos = usize(-1);

} // namespace sgbd
